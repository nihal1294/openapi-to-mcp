minimum_pre_commit_version: "4.0.0"
default_install_hook_types:
  - pre-commit
  - pre-push

repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: uv run ruff format
        language: system
        types: [python]
      - id: ruff-check
        name: ruff check --fix
        entry: uv run ruff check --fix
        language: system
        types: [python]
      - id: pytest
        name: pytest
        entry: uv run pytest --cov=openapi_to_mcp --cov-report=term-missing
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
